type Query {
    name: String
}

"Subgraph 1 description"
scalar Email

"Subgraph 1 description"
union Foo = Bar

"Subgraph 1 description"
type Bar {
    name: String
}

"Subgraph 3 description"
interface Bux {
    name: String
}

"Subgraph 1 description"
enum Waldo {
    COMPLETE
    FAIL
}