type Query {
    foo: String
}

"Subgraph 1 description"
scalar Email

"Subgraph 3 description"
union Foo = Bar

type Bar {
    name: String
}

"Subgraph 3 description"
interface Bux {
    name: String
}

"Subgraph 3 description"
enum Waldo {
    COMPLETE
    FAIL
}