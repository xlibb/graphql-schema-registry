extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.3", import: ["@key"])

type Query {
    name: String
}

type Bar {
    name: String
    foo: Foo
}

type Foo @key(fields: "he", resolvable: false) {
    id: ID!
    he: String
}