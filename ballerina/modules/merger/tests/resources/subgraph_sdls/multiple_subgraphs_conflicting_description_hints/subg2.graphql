type Query {
    name: String
    waldo("Subgraph 2 Description" input: Boolean): String
    bar: Int
    thud(input: ID!): String
}

interface Foo {
    name: String
}

interface Bar {
    name: String
}